# Copy this file to .env.local and fill in real values.
# Not used yet on the homepage — will be needed once the Contact form (Step 2) is built.

NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
# Server-only key, used in the form submit route/edge function, never expose to the client.
SUPABASE_SERVICE_ROLE_KEY=

# Email notification service for new lead submissions (Step 2)
RESEND_API_KEY=
NOTIFY_EMAIL_TO=hello@solarleadads.com
